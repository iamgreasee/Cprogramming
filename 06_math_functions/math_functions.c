#include <stdio.h>
#include <math.h>

int main() {
	
	//--- SQUARE ROOT ---
	//int x = 9;
	//x = sqrt(x);	// this requires the -lm flag when compiling. the math header file needs linking as it is not a part of the standard c library
	//printf("%d\n", x);
	
	//--- TO THE POWER OF ---
	//int x = 2; 
	//x = pow(x, 2);	// The pow function takes a base and and raises it to the given power.
			// the pow function takes the first number as a base and the second as the power. 
	//printf("%d\n", x);
	
	//--- ROUND UP/DOWN ---
	//float x = 3.14;

	//x = round(x);	// here the value of x is set to the value of x rounded up. 

	//x = ciel(x);   // what the cieling function does is that it rounds up no matter the range of the value, 
			// so even if a value was equal to 3.1, the ciel funciton will make x be equal t 4.
	
	//x = floor(x);	//what the floor function does is that it rounds down no matter the range of the value, 
			//so if a value was equal to 3.9, the floow function will make it be equal to 3.
	
	//x = abs(x);	// the absolute function will give you the distance from 0. 
			// so lets say x is equal to -3, the abs function will return 3. 

	//x = log(x);	// gives you the natural logarithm of the value within the function.
	
	//x = sin(x);	// gives you the sin value of a value within the sin function. 
	
	//x = cos(x);	// gives you the cosin of a value passed within the cos function. 
	
	//x = tan(x);	// gives you the tangent of the value passed within the tan function. 
	
	//printf("%f\n", x); 

	return 0;
}
